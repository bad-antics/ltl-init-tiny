// examples/demo.ltl — runnable demo for ltl_init_tiny

import std::io
use ltl_init_tiny as lib

fn main() {
    io::println("ltl_init_tiny demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}